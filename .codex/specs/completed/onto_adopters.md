Task:
- add two extra field for ontology adopters feature

Process:
- on each onotology page, there is functionality to render ontology adopters (if exists) in a modal
- render two extra fields (if exists) for each adopters
    - created
    - source
- these fields exists nested under `usageReportMetadata` metadata

Acceptance Criteria:
- ask me to check.
