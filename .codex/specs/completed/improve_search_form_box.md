Task: improve the search form jumpto and autocomplete boxes

Goals:

- both boxes must be closed when 
   - the search input is empty
   - when user press the ESC key
   - triggers the search 

- The boxes must the the same width as the search input on all screen sizes
- in the jump to box, the keyboard navigation must work also based on left/right arrows beside up/down.
- make both boxes style glassmorphism
- Put a vertical like with with style between term and ontology jump to boxes


Acceptance Criteria:
- check typescript errors. I will test the UI myself.
