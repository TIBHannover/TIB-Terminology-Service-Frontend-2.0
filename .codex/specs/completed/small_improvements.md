Task1: 
- On the Info tab in the site navbar `Navbar.tsx`, when I click on an options in its dropdown, for a moment the options background color becomes blue. remove this effect.

Task2:
- add loading for termset list in `TermsetList.tsx` and `UserTermsetList.tsx` before the items loads. 
- make the termset item show up with a fade effect instead of suddenly appearing.

Accpetance Criteria:
- Only check  typescript errors and warnings. 
- Tell me I test the changes myself. 
- Do not build the project.
